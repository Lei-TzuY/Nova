fn choose(flag: Bool, left: Int, right: Int) -> Int {
    let selected = if flag {
        left
    } else {
        right
    };

    selected + 1
}

fn main() -> Int {
    var seed: Int = 40;
    seed = seed + 1;
    choose(seed > 0 && true, seed, 0)
}
