fn main() -> Int {
    var index = 0;
    var sum = 0;
    while index < 7 {
        index = index + 1;
        if index == 2 { continue; } else { 0 };
        if index == 6 { break; } else { 0 };
        sum = sum + index;
    }
    sum
}
