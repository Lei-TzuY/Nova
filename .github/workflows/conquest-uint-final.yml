name: Conquest UInt final gates

on:
  push:
    branches: [bil-5-uint-family]

permissions:
  contents: write

jobs:
  validate:
    if: ${{ !contains(github.event.head_commit.message, '[uint-final-result]') }}
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: bil-5-uint-family
          fetch-depth: 0
      - uses: dtolnay/rust-toolchain@stable
        with:
          components: rustfmt, clippy
      - uses: dtolnay/rust-toolchain@1.85.0
      - name: Validate and clean staging helpers
        shell: bash
        run: |
          cargo fmt --all
          set +e
          : > conquest-result.log
          run_gate() { echo "### $*" >> conquest-result.log; "$@" >> conquest-result.log 2>&1; rc=$?; echo "exit=$rc" >> conquest-result.log; return $rc; }
          failed=0
          run_gate cargo fmt --all -- --check || failed=1
          run_gate cargo clippy --workspace --all-targets --all-features --locked -- -D warnings || failed=1
          run_gate cargo +1.85.0 check --workspace --all-targets --locked || failed=1
          run_gate cargo test --workspace --all-targets --locked || failed=1
          run_gate cargo build --workspace --all-targets --locked || failed=1
          echo '### cargo doc' >> conquest-result.log
          RUSTDOCFLAGS='-D warnings' cargo doc --workspace --no-deps --locked >> conquest-result.log 2>&1 || failed=1
          echo "overall_failed=$failed" >> conquest-result.log
          git config user.name 'LeiZ'
          git config user.email '52287354+Lei-TzuY@users.noreply.github.com'
          if [ "$failed" -eq 0 ]; then
            rm -f conquest-result.log .github/workflows/conquest-uint.yml .github/workflows/conquest-uint-fix.yml .github/workflows/conquest-uint-fix2.yml .github/workflows/conquest-uint-fix3.yml .github/workflows/conquest-uint-final.yml
            git add -A
            git commit -m '[uint-final-result] feat: complete checked UInt numeric family'
            git push origin HEAD:bil-5-uint-family
          else
            git add -A
            git commit -m '[uint-final-result] chore: capture final UInt gate diagnostics'
            git push origin HEAD:bil-5-uint-family
            exit 1
          fi