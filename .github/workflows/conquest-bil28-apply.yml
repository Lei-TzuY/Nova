name: Conquest BIL-28 apply

on:
  push:
    branches:
      - conquest/bil-28-shared-mutable-capture

permissions:
  contents: write

jobs:
  apply:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: conquest/bil-28-shared-mutable-capture
          fetch-depth: 0
      - name: Apply implementation
        run: python3 scripts/bil28_apply.py
      - name: Format
        run: cargo fmt --all
      - name: Test workspace
        run: cargo test --workspace --all-targets
      - name: Clippy
        run: cargo clippy --workspace --all-targets -- -D warnings
      - name: Commit implementation
        shell: bash
        run: |
          rm -f scripts/bil28_apply.py .github/workflows/conquest-bil28-apply.yml
          git config user.name 'LeiZ'
          git config user.email '52287354+Lei-TzuY@users.noreply.github.com'
          git add -A
          git commit -m 'feat: add shared mutable closure cells'
          git push origin HEAD:conquest/bil-28-shared-mutable-capture
