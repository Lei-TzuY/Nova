name: Validate invalid record head flow fix

on:
  push:
    branches:
      - fix/invalid-record-head-flow

permissions:
  contents: write

jobs:
  fix:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: fix/invalid-record-head-flow
      - uses: dtolnay/rust-toolchain@stable
        with:
          components: rustfmt,clippy
      - name: Confirm new regression is red
        shell: bash
        run: |
          if cargo test -p nova-sema --test invalid_aggregate_flow --locked; then
            echo "expected invalid-record-head regressions to fail before the fix" >&2
            exit 1
          fi
      - name: Apply record-head recovery fix
        run: python3 scripts/apply_invalid_record_head_flow.py
      - name: Validate fixed semantic slice
        run: |
          cargo fmt --all
          cargo test -p nova-sema --all-targets --locked
          cargo clippy -p nova-sema --all-targets --all-features -- -D warnings
      - name: Commit fix and remove helpers
        run: |
          rm .github/workflows/invalid-record-head-flow.yml scripts/apply_invalid_record_head_flow.py
          git config user.name "LeiZ"
          git config user.email "52287354+Lei-TzuY@users.noreply.github.com"
          git add -A
          git commit -m "fix: fail closed on invalid record heads"
          git push origin HEAD:fix/invalid-record-head-flow
