name: BIL-37 implementation

on:
  push:
    branches:
      - conquest/bil-37-explicit-generic-arguments

permissions:
  contents: write

jobs:
  implement:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Apply parser patch
        run: python3 .github/bil37_patch.py
      - name: Format and persist
        run: |
          cargo fmt --all
          git config user.name LeiZ
          git config user.email 52287354+Lei-TzuY@users.noreply.github.com
          git add crates/nova-parser/src/parser.rs
          git commit -m "feat: parse explicit generic call arguments"
          git push origin HEAD:conquest/bil-37-explicit-generic-arguments
