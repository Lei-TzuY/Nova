name: Replay type rules on aggregate-flow main

on:
  push:
    branches:
      - refactor/type-rules-v2

permissions:
  contents: write

jobs:
  replay:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: refactor/type-rules-v2
      - uses: dtolnay/rust-toolchain@stable
        with:
          components: rustfmt,clippy
      - name: Apply type-rules refactor
        run: python3 scripts/apply_type_rules_v2.py
      - name: Validate semantic crate and aggregate rollback
        run: |
          cargo fmt --all
          cargo test -p nova-sema --all-targets --locked
          cargo clippy -p nova-sema --all-targets --all-features -- -D warnings
      - name: Commit refactor and remove helpers
        run: |
          rm .github/workflows/type-rules-v2.yml
          rm scripts/apply_type_rules_v2.py
          git config user.name "LeiZ"
          git config user.email "52287354+Lei-TzuY@users.noreply.github.com"
          git add -A
          git commit -m "refactor: centralize semantic type rules"
          git push origin HEAD:refactor/type-rules-v2
